
{
  "entities": {
    "Service": {
      "title": "Service",
      "description": "A service package offered by the business.",
      "type": "object",
      "properties": {
        "name": { "type": "string" },
        "price": { "type": "string" },
        "description": { "type": "string" },
        "iconName": { "type": "string" },
        "imageUrl": { "type": "string", "description": "Base64 or URL of the service image." },
        "features": { "type": "array", "items": { "type": "string" } },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["name", "price", "description", "iconName"]
    },
    "Booking": {
      "title": "Booking",
      "description": "A service request from a customer.",
      "type": "object",
      "properties": {
        "fullName": { "type": "string" },
        "whatsapp": { "type": "string" },
        "address": { "type": "string" },
        "service": { "type": "string" },
        "notes": { "type": "string" },
        "status": { "type": "string", "enum": ["Pending", "Selesai"] },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["fullName", "whatsapp", "address", "service", "status"]
    },
    "BusinessSettings": {
      "title": "BusinessSettings",
      "description": "General settings for the business content and branding.",
      "type": "object",
      "properties": {
        "name": { "type": "string" },
        "whatsapp": { "type": "string" },
        "address": { "type": "string" },
        "email": { "type": "string" },
        "mapEmbedUrl": { "type": "string", "description": "The src URL from Google Maps Embed iframe code." },
        "mapDirectUrl": { "type": "string", "description": "The direct link to Google Maps for navigation." },
        "logoText": { "type": "string" },
        "logoAccentText": { "type": "string" },
        "logoUrl": { "type": "string" },
        "heroBadge": { "type": "string", "description": "Short text shown in the hero badge." },
        "heroTitle": { "type": "string" },
        "heroSubtitle": { "type": "string" },
        "heroImageUrl": { "type": "string", "description": "Base64 or URL of the hero background image." },
        "aboutTitle": { "type": "string" },
        "aboutContent": { "type": "string" },
        "servicesSectionBadge": { "type": "string" },
        "servicesSectionTitle": { "type": "string" },
        "servicesSectionSubtitle": { "type": "string" },
        "privacyPolicy": { "type": "string" },
        "socialInstagram": { "type": "string" },
        "socialFacebook": { "type": "string" },
        "socialYoutube": { "type": "string" },
        "socialTiktok": { "type": "string" }
      }
    }
  },
  "auth": {
    "providers": ["email"]
  },
  "firestore": {
    "/services/{serviceId}": {
      "schema": "Service",
      "description": "Collection of available service packages."
    },
    "/bookings/{bookingId}": {
      "schema": "Booking",
      "description": "Collection of customer bookings."
    },
    "/settings/business": {
      "schema": "BusinessSettings",
      "description": "Singleton document for business settings."
    }
  }
}
